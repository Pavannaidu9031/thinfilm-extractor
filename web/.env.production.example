# ── Frontend production environment (Vercel / Netlify) ─────────────────────
# The ONLY frontend env var. It is the public URL of your deployed backend —
# it is NOT a secret (the Gemini key and DB string never touch the frontend).
VITE_API_URL=https://your-backend.onrender.com
